Blank Space v132

Fixes:
- Website unlock code can be typed with keyboard numbers again.
- Lock keypad can be clicked/tapped again.
- Removes heavy page observers that could make Chrome say the page is not responding.
- Disables automatic helper-tab opening during initial page load so it cannot freeze the tab.
- No Supabase SQL needed.

Upload this ZIP to GitHub Pages and hard refresh with Ctrl+Shift+R.
