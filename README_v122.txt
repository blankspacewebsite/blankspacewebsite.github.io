Blank Space v122

Changes:
- Removed the manual in-game Achievement Sync bar from v121.
- Removed the input where users could type fake achievement progress.
- Achievements are now automatic-only again.
- Keeps the v121 backend/catalog fixes and automatic detectors.
- No new Supabase SQL is required if v121 SQL was already run.
