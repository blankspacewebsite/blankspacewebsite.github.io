Blank Space v152 - No Supabase SQL needed

This update is front-end only.

Fixes:
- Removes the false "Website banned" overlay.
- Removes the broken raw template text: ${escapeHTML(r.data.reason||'')}.
- Disables the old device ban check that could false-trigger on a fresh Supabase project.

Do not run any SQL for v152.
