Blank Space v111

Changes:
- Fixed the unlock-code screen so users can type the code into a real numeric input.
- Keypad buttons now use stronger click/touch/pointer handlers.
- Keyboard entry, Backspace, Enter, and Escape work on the unlock screen.
- Block Protection close/refresh warning does not run while the unlock screen is still locked.
- No Supabase changes required if v110 SQL was already run.
